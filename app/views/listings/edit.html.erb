<div class="page-header">
  <h1>Edit Listing</h1>
</div>

<div class="card">
  <%= form_for @listing, url: listing_path(@listing.id), method: :put, html: { multipart: true } do |f| %>
    <%= render partial: 'listings/form', locals: { f: f } %>
  <% end %>
</div>